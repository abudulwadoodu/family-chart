services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-familytree}
      POSTGRES_USER: ${POSTGRES_USER:-familytree_app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-familytree_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
